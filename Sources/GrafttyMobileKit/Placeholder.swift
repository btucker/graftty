#if canImport(UIKit)
import Foundation

/// Placeholder so the SwiftPM target has at least one source file during
/// the scaffolding wave. Later tasks replace this with real implementation
/// files; this file itself is deleted in Task 14.
enum GrafttyMobileKitPlaceholder {}
#endif
