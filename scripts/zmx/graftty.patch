diff --git a/src/cfg.zig b/src/cfg.zig
index bec7963..4db0e44 100644
--- a/src/cfg.zig
+++ b/src/cfg.zig
@@ -9,7 +9,11 @@ const cross = @import("cross.zig");

 socket_dir: []const u8,
 log_dir: []const u8,
-max_scrollback_lines: usize = 2_000, // same default as tmux
+// Preserve Graftty's pre-snapshot 10 MB retention budget. The experimental
+// branch otherwise falls back to Ghostty's 10 KB default while adding a line
+// cap, which discards most of a 10,000-line session.
+max_scrollback_bytes: usize = 10_000_000,
+max_scrollback_lines: usize = 10_000_000,
 dir_mode: u32 = 0o750,
 log_mode: u32 = 0o640,
 tracked_envs: []const u8 = "DISPLAY,SSH_AUTH_SOCK,SSH_AGENT_PID,SSH_CONNECTION,WINDOWID,XAUTHORITY,KITTY_LISTEN_ON,KITTY_PID,KITTY_WINDOW_ID",
diff --git a/src/daemonize.zig b/src/daemonize.zig
index 800431b..80c7124 100644
--- a/src/daemonize.zig
+++ b/src/daemonize.zig
@@ -86,7 +86,7 @@ pub const PtyInfo = struct {
 ///
 /// This is the second fork in the double-fork technique explained in the
 /// daemonize() comment.
-pub fn spawnPty(sesh_name: []const u8, cmd: Cmd, size: ipc.Resize) !PtyInfo {
+pub fn spawnPty(sesh_name: []const u8, cmd: Cmd, size: ipc.TerminalSize) !PtyInfo {
     var ws: cross.c.struct_winsize = .{
         .ws_row = size.rows,
         .ws_col = size.cols,
diff --git a/src/ipc.zig b/src/ipc.zig
index a43b5a7..4bf7951 100644
--- a/src/ipc.zig
+++ b/src/ipc.zig
@@ -27,6 +27,9 @@ pub const Tag = enum(u8) {
     EnvSet = 20,
     EnvData = 21,
     Snapshot = 22,
+    Capabilities = 23,
+    PixelSize = 24,
+    Tail = 25,
     // Non-exhaustive: this enum comes off the wire via bytesToValue and
     // @enumFromInt, so out-of-range values are representable
     // rather than UB. Switches must handle `_` (unknown tag).
@@ -47,15 +50,58 @@ pub const Header = packed struct {
 pub const Resize = packed struct {
     rows: u16,
     cols: u16,
+};
+
+/// The pre-Graftty snapshot branch encoded pixel dimensions in Init/Resize.
+/// Accept this shape in Init so stock experimental clients can still attach.
+pub const LegacyResize = packed struct {
+    rows: u16,
+    cols: u16,
+    xpixel: u16 = 0,
+    ypixel: u16 = 0,
+};
+
+pub const PixelSize = packed struct {
+    xpixel: u16 = 0,
+    ypixel: u16 = 0,
+};
+
+pub const Capabilities = packed struct {
+    bits: u64 = 0,
+
+    pub const pixel_size: u64 = 1 << 0;
+    pub const snapshot: u64 = 1 << 1;
+    pub const raw_snapshot_stream: u64 = 1 << 2;
+
+    pub fn supportsPixelSize(self: Capabilities) bool {
+        return self.bits & pixel_size != 0;
+    }
+
+    pub fn supportsSnapshot(self: Capabilities) bool {
+        return self.bits & snapshot != 0;
+    }
+
+    pub fn supportsRawSnapshotStream(self: Capabilities) bool {
+        return self.bits & raw_snapshot_stream != 0;
+    }
+};
+
+pub const TerminalSize = struct {
+    rows: u16,
+    cols: u16,
     xpixel: u16 = 0,
     ypixel: u16 = 0,

-    pub fn winsize(self: Resize) cross.c.struct_winsize {
-        return .{ .ws_row = self.rows, .ws_col = self.cols, .ws_xpixel = self.xpixel, .ws_ypixel = self.ypixel };
+    pub fn resize(self: TerminalSize) Resize {
+        return .{ .rows = self.rows, .cols = self.cols };
+    }
+
+    pub fn pixelSize(self: TerminalSize) PixelSize {
+        return .{ .xpixel = self.xpixel, .ypixel = self.ypixel };
     }
 };

-pub fn getTerminalSize(fd: i32) Resize {
+pub fn getTerminalSize(fd: i32) TerminalSize {
     var ws: cross.c.struct_winsize = undefined;
     if (cross.c.ioctl(fd, cross.c.TIOCGWINSZ, &ws) == 0 and ws.ws_row > 0 and ws.ws_col > 0) {
         return .{ .rows = ws.ws_row, .cols = ws.ws_col, .xpixel = ws.ws_xpixel, .ypixel = ws.ws_ypixel };
@@ -76,6 +122,29 @@ pub fn getTerminalSize(fd: i32) Resize {
     return .{ .rows = 24, .cols = 120 };
 }

+test "Graftty wire extensions preserve legacy resize size" {
+    try std.testing.expectEqual(@as(usize, 4), @sizeOf(Resize));
+    try std.testing.expectEqual(@as(usize, 8), @sizeOf(LegacyResize));
+    try std.testing.expectEqual(@as(usize, 4), @sizeOf(PixelSize));
+    try std.testing.expectEqual(@as(usize, 8), @sizeOf(Capabilities));
+    try std.testing.expectEqual(@as(u8, 22), @intFromEnum(Tag.Snapshot));
+    try std.testing.expectEqual(@as(u8, 23), @intFromEnum(Tag.Capabilities));
+    try std.testing.expectEqual(@as(u8, 24), @intFromEnum(Tag.PixelSize));
+    try std.testing.expectEqual(@as(u8, 25), @intFromEnum(Tag.Tail));
+
+    const capabilities = Capabilities{
+        .bits = Capabilities.pixel_size |
+            Capabilities.snapshot |
+            Capabilities.raw_snapshot_stream,
+    };
+    try std.testing.expect(capabilities.supportsPixelSize());
+    try std.testing.expect(capabilities.supportsSnapshot());
+    try std.testing.expect(capabilities.supportsRawSnapshotStream());
+    try std.testing.expect(!(Capabilities{}).supportsPixelSize());
+    try std.testing.expect(!(Capabilities{}).supportsSnapshot());
+    try std.testing.expect(!(Capabilities{}).supportsRawSnapshotStream());
+}
+
 pub const MAX_CMD_LEN = 256;
 pub const MAX_CWD_LEN = 256;

@@ -322,13 +391,15 @@ test "Info wire size is frozen" {

 test "Tag wire values are frozen" {
     inline for (.{
-        .{ Tag.Input, 0 },     .{ Tag.Output, 1 },        .{ Tag.Resize, 2 },
-        .{ Tag.Detach, 3 },    .{ Tag.DetachAll, 4 },     .{ Tag.Kill, 5 },
-        .{ Tag.Info, 6 },      .{ Tag.Init, 7 },          .{ Tag.History, 8 },
-        .{ Tag.Run, 9 },       .{ Tag.Ack, 10 },          .{ Tag.Switch, 11 },
-        .{ Tag.Write, 12 },    .{ Tag.TaskComplete, 13 }, .{ Tag.LabelGet, 14 },
-        .{ Tag.LabelSet, 15 }, .{ Tag.LabelClear, 16 },   .{ Tag.LabelData, 17 },
-        .{ Tag.Send, 18 },
+        .{ Tag.Input, 0 },      .{ Tag.Output, 1 },        .{ Tag.Resize, 2 },
+        .{ Tag.Detach, 3 },     .{ Tag.DetachAll, 4 },     .{ Tag.Kill, 5 },
+        .{ Tag.Info, 6 },       .{ Tag.Init, 7 },          .{ Tag.History, 8 },
+        .{ Tag.Run, 9 },        .{ Tag.Ack, 10 },          .{ Tag.Switch, 11 },
+        .{ Tag.Write, 12 },     .{ Tag.TaskComplete, 13 }, .{ Tag.LabelGet, 14 },
+        .{ Tag.LabelSet, 15 },  .{ Tag.LabelClear, 16 },   .{ Tag.LabelData, 17 },
+        .{ Tag.Send, 18 },      .{ Tag.EnvGet, 19 },       .{ Tag.EnvSet, 20 },
+        .{ Tag.EnvData, 21 },   .{ Tag.Snapshot, 22 },     .{ Tag.Capabilities, 23 },
+        .{ Tag.PixelSize, 24 }, .{ Tag.Tail, 25 },
     }) |p| try std.testing.expectEqual(@as(u8, p[1]), @intFromEnum(p[0]));
 }

diff --git a/src/loop.zig b/src/loop.zig
index 4509ec0..3b5678a 100644
--- a/src/loop.zig
+++ b/src/loop.zig
@@ -20,7 +20,6 @@ pub fn clientLoop(
     io: std.Io,
     client_sock_fd: i32,
     snapshot_mode: bool,
-    init_snapshot: []const u8,
     env_str: []const u8,
 ) !ClientResult {
     std.log.info("client loop fd={d}", .{client_sock_fd});
@@ -36,6 +35,10 @@ pub fn clientLoop(
     };
     defer lib_posix.close(client_sock_fd);

+    var sigwinch_mask = lib_posix.sigemptyset();
+    lib_posix.sigaddset(&sigwinch_mask, lib_posix.SIG.WINCH);
+    lib_posix.sigprocmask(lib_posix.SIG.UNBLOCK, &sigwinch_mask, null);
+
     try signal.openSignalPipe();
     signal.installWakeHandler(@intFromEnum(lib_posix.SIG.WINCH));

@@ -52,8 +55,26 @@ pub fn clientLoop(
         try ipc.appendMessage(gpa, &sock_write_buf, .EnvSet, env_str);
     }

-    // Send init message with terminal snapshot (buffered)
-    try ipc.appendMessage(gpa, &sock_write_buf, .Init, init_snapshot);
+    // Advertise snapshot support before Init so a new daemon can select the
+    // binary transport. Old daemons ignore the unknown tag and still receive
+    // the frozen four-byte Init payload.
+    const client_capabilities = ipc.Capabilities{
+        .bits = ipc.Capabilities.pixel_size |
+            ipc.Capabilities.snapshot |
+            (if (snapshot_mode) ipc.Capabilities.raw_snapshot_stream else 0),
+    };
+    try ipc.appendMessage(
+        gpa,
+        &sock_write_buf,
+        .Capabilities,
+        std.mem.asBytes(&client_capabilities),
+    );
+    const size = ipc.getTerminalSize(lib_posix.STDOUT_FILENO);
+    const initial_resize = size.resize();
+    try ipc.appendMessage(gpa, &sock_write_buf, .Init, std.mem.asBytes(&initial_resize));
+    var pixel_protocol: PixelProtocol = .none;
+    var server_supports_snapshot = false;
+    var received_snapshot = false;

     var poll_fds = try std.ArrayList(lib_posix.pollfd).initCapacity(gpa, 4);
     defer poll_fds.deinit(gpa);
@@ -110,7 +131,7 @@ pub fn clientLoop(
         if (poll_fds.items[2].revents & lib_posix.POLL.IN != 0) {
             signal.drainSignalPipe();
             const next_size = ipc.getTerminalSize(lib_posix.STDOUT_FILENO);
-            try ipc.appendMessage(gpa, &sock_write_buf, .Resize, std.mem.asBytes(&next_size));
+            try appendTerminalSize(gpa, &sock_write_buf, next_size, pixel_protocol);
         }

         // Handle stdin -> socket (Input)
@@ -161,11 +182,29 @@ pub fn clientLoop(
                 switch (msg.header.tag) {
                     .Output => {
                         if (msg.payload.len > 0) {
+                            // A silent socket can temporarily look like a
+                            // pre-Tail-tag client. Once a current daemon has
+                            // advertised snapshots, output received before
+                            // the snapshot is stale classification-window
+                            // output and is represented by the snapshot.
+                            if (server_supports_snapshot and !received_snapshot) continue;
+                            if (snapshot_mode and !received_snapshot) return error.SnapshotUnsupported;
                             try stdout_buf.appendSlice(gpa, msg.payload);
                         }
                     },
                     .Snapshot => {
-                        if (msg.payload.len > 0) {
+                        if (msg.payload.len == 0) {
+                            if (snapshot_mode) return error.SnapshotUnsupported;
+                            server_supports_snapshot = false;
+                        } else {
+                            // Tag 22 is upstream's Snapshot frame. Reject
+                            // anything without its envelope before writing to
+                            // a raw binary stream.
+                            if (!std.mem.startsWith(u8, msg.payload, "GHOSTSNP")) {
+                                std.log.debug("ignoring non-snapshot tag 22 payload", .{});
+                                continue;
+                            }
+                            received_snapshot = true;
                             if (snapshot_mode) {
                                 try stdout_buf.appendSlice(gpa, msg.payload);
                             } else {
@@ -182,6 +221,10 @@ pub fn clientLoop(
                                         defer if (restore_data.ptr != term_output.ptr) gpa.free(restore_data);
                                         try stdout_buf.appendSlice(gpa, restore_data);
                                     }
+                                    switch (mut_res.continuation) {
+                                        .ground => {},
+                                        .bytes => |bytes| try stdout_buf.appendSlice(gpa, bytes),
+                                    }
                                 } else |err| {
                                     std.log.warn("failed to import snapshot in client err={s}", .{@errorName(err)});
                                 }
@@ -192,12 +235,25 @@ pub fn clientLoop(
                         // daemon is asking for the client's window size usually in response
                         // to this client being set as leader.
                         const next_size = ipc.getTerminalSize(lib_posix.STDOUT_FILENO);
-                        try ipc.appendMessage(
-                            gpa,
-                            &sock_write_buf,
-                            .Resize,
-                            std.mem.asBytes(&next_size),
-                        );
+                        try appendTerminalSize(gpa, &sock_write_buf, next_size, pixel_protocol);
+                    },
+                    .Capabilities => {
+                        if (msg.payload.len != @sizeOf(ipc.Capabilities)) continue;
+                        const capabilities = std.mem.bytesToValue(ipc.Capabilities, msg.payload);
+                        if (capabilities.supportsPixelSize()) pixel_protocol = .current;
+                        if (capabilities.supportsSnapshot()) server_supports_snapshot = true;
+                    },
+                    .Send => {
+                        // Graftty 0.6 used tag 18 for the daemon-to-client
+                        // Capabilities message before upstream assigned that
+                        // value to Send. Servers never send a real Send frame,
+                        // so direction and payload size disambiguate it.
+                        if (pixel_protocol == .none and
+                            msg.payload.len == @sizeOf(ipc.Capabilities))
+                        {
+                            const capabilities = std.mem.bytesToValue(ipc.Capabilities, msg.payload);
+                            if (capabilities.supportsPixelSize()) pixel_protocol = .legacy;
+                        }
                     },
                     .Switch => {
                         std.log.info("switch session", .{});
@@ -251,6 +307,64 @@ pub fn clientLoop(
     }
 }

+const PixelProtocol = enum { none, legacy, current };
+
+fn appendTerminalSize(
+    gpa: std.mem.Allocator,
+    write_buf: *std.ArrayList(u8),
+    size: ipc.TerminalSize,
+    pixel_protocol: PixelProtocol,
+) !void {
+    const pixels = size.pixelSize();
+    switch (pixel_protocol) {
+        .none => {},
+        .legacy => try ipc.appendMessage(gpa, write_buf, .EnvGet, std.mem.asBytes(&pixels)),
+        .current => try ipc.appendMessage(gpa, write_buf, .PixelSize, std.mem.asBytes(&pixels)),
+    }
+    const resize = size.resize();
+    try ipc.appendMessage(gpa, write_buf, .Resize, std.mem.asBytes(&resize));
+}
+
+fn appendTerminalSnapshot(
+    gpa: std.mem.Allocator,
+    client: *Client,
+    term: *const ghostty_vt.Terminal,
+    vt_stream: *const ghostty_vt.TerminalStream,
+) !void {
+    var continuation_buf: std.Io.Writer.Allocating = .init(gpa);
+    defer continuation_buf.deinit();
+    if (!vt_stream.ground()) try vt_stream.writeContinuation(&continuation_buf.writer);
+
+    var snap_buf: std.Io.Writer.Allocating = .init(gpa);
+    defer snap_buf.deinit();
+    const continuation: snapshot.Continuation = if (continuation_buf.written().len == 0)
+        .ground
+    else
+        .{ .bytes = continuation_buf.written() };
+    try snapshot.exportSnapshot(gpa, &snap_buf.writer, term, .{
+        .continuation = continuation,
+    });
+    try ipc.appendMessage(gpa, &client.write_buf, .Snapshot, snap_buf.written());
+    client.has_pending_output = true;
+    std.log.debug("serialize terminal snapshot bytes={d}", .{snap_buf.written().len});
+}
+
+fn appendTerminalContinuation(
+    gpa: std.mem.Allocator,
+    client: *Client,
+    vt_stream: *const ghostty_vt.TerminalStream,
+) !void {
+    if (vt_stream.ground()) return;
+
+    var continuation_buf: std.Io.Writer.Allocating = .init(gpa);
+    defer continuation_buf.deinit();
+    try vt_stream.writeContinuation(&continuation_buf.writer);
+    if (continuation_buf.written().len == 0) return;
+
+    try ipc.appendMessage(gpa, &client.write_buf, .Output, continuation_buf.written());
+    client.has_pending_output = true;
+}
+
 /// dameonLoop is what the daemon runs to send and receive ipc commands from its corresponding
 /// clients.  It uses poll() as its non-blocking mechanism.
 fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_fd: lib_posix.socket_t, pty_fd: i32) !void {
@@ -265,10 +379,15 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
     var term = try ghostty_vt.Terminal.init(io, gpa, .{
         .cols = init_size.cols,
         .rows = init_size.rows,
+        .max_scrollback_bytes = daemon.cfg.max_scrollback_bytes,
         .max_scrollback_lines = daemon.cfg.max_scrollback_lines,
     });
     defer term.deinit(gpa);
-    var vt_stream = term.vtStream();
+    var vt_stream = ghostty_vt.TerminalStream.init(.{
+        .allocator = gpa,
+        .handler = term.vtHandler(),
+        .continuation_max_bytes = 1024 * 1024,
+    });
     defer vt_stream.deinit();

     // Carries the tail of the previous PTY read so the task-exit marker
@@ -309,7 +428,20 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f

         try poll_fds.append(gpa, .{ .fd = signal.sig_pipe[0], .events = lib_posix.POLL.IN, .revents = 0 });

+        var poll_timeout_ms: i32 = -1;
+        const now_ms = std.Io.Timestamp.now(io, .awake).toMilliseconds();
         for (daemon.clients.items) |client| {
+            if (client.pty_output_state == .classifying and client.legacy_tail_candidate) {
+                const remaining = client.legacy_tail_ready_at_ms - now_ms;
+                if (remaining <= 0) {
+                    poll_timeout_ms = 0;
+                } else {
+                    poll_timeout_ms = @intCast(if (poll_timeout_ms < 0)
+                        remaining
+                    else
+                        @min(remaining, poll_timeout_ms));
+                }
+            }
             var events: i16 = lib_posix.POLL.IN;
             if (client.has_pending_output) {
                 events |= lib_posix.POLL.OUT;
@@ -321,7 +453,11 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
             });
         }

-        _ = try lib_posix.poll(poll_fds.items, -1);
+        _ = try lib_posix.poll(poll_fds.items, poll_timeout_ms);
+
+        // poll_fds contains [server, pty, sig_pipe, client0, client1, ...].
+        // A client accepted below is not represented until the next loop.
+        const num_polled_clients = poll_fds.items.len - 3;

         if (poll_fds.items[2].revents & lib_posix.POLL.IN != 0) {
             signal.drainSignalPipe();
@@ -346,6 +482,7 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
             client.* = Client{
                 .alloc = gpa,
                 .socket_fd = client_fd,
+                .legacy_tail_ready_at_ms = std.Io.Timestamp.now(io, .awake).toMilliseconds() + 1000,
                 .read_buf = try ipc.SocketBuffer.init(gpa),
                 .write_buf = undefined,
             };
@@ -354,6 +491,16 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
             // buffer is userspace-only: it drains via POLLOUT to the client socket,
             // which has no corresponding kernel-imposed per-write limit.
             client.write_buf = try std.ArrayList(u8).initCapacity(client.alloc, 65536);
+            const server_capabilities = ipc.Capabilities{
+                .bits = ipc.Capabilities.pixel_size | ipc.Capabilities.snapshot,
+            };
+            try ipc.appendMessage(
+                gpa,
+                &client.write_buf,
+                .Capabilities,
+                std.mem.asBytes(&server_capabilities),
+            );
+            client.has_pending_output = true;
             try daemon.clients.append(gpa, client);
             std.log.info(
                 "client connected fd={d} total={d}",
@@ -361,6 +508,23 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
             );
         }

+        const classification_now_ms = std.Io.Timestamp.now(io, .awake).toMilliseconds();
+        for (daemon.clients.items[0..num_polled_clients], 0..) |client, client_i| {
+            const revents = poll_fds.items[client_i + 3].revents;
+            const has_socket_event = revents &
+                (lib_posix.POLL.IN | lib_posix.POLL.HUP | lib_posix.POLL.ERR | lib_posix.POLL.NVAL) != 0;
+            if (client.pty_output_state == .classifying and
+                client.legacy_tail_candidate and
+                classification_now_ms >= client.legacy_tail_ready_at_ms and
+                !has_socket_event)
+            {
+                // Pre-Tail-tag clients subscribe by opening a socket and
+                // remaining silent. Replay everything buffered during the
+                // classification window once silence identifies that client.
+                try client.startReceivingPtyOutput(gpa, true);
+            }
+        }
+
         const inp_flags = lib_posix.POLL.IN | lib_posix.POLL.HUP | lib_posix.POLL.ERR | lib_posix.POLL.NVAL;
         if (poll_fds.items[1].revents & inp_flags != 0) {
             // Read from PTY. Buffer is sized to N_TTY_BUF_SIZE (4096): the hard
@@ -434,12 +598,11 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
                     const broadcast_data = util.rewritePromptRedraw(gpa, buf[0..n]) orelse buf[0..n];
                     defer if (broadcast_data.ptr != buf[0..n].ptr) gpa.free(broadcast_data);
                     for (daemon.clients.items) |client| {
-                        ipc.appendMessage(gpa, &client.write_buf, .Output, broadcast_data) catch |err| {
+                        client.queuePtyOutput(gpa, broadcast_data) catch |err| {
                             std.log.warn(
                                 "failed to buffer output for client err={s}",
                                 .{@errorName(err)},
                             );
                             continue;
                         };
-                        client.has_pending_output = true;
                     }
@@ -465,7 +629,6 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
         // Only iterate over clients that were present when poll_fds was constructed
         // poll_fds contains [server, pty, sig_pipe, client0, client1, ...]
         // So number of clients in poll_fds is poll_fds.items.len - 3
-        const num_polled_clients = poll_fds.items.len - 3;
         if (i > num_polled_clients) {
             // If we have more clients than polled (i.e. we just accepted one), start from the
             // polled ones
@@ -495,13 +658,26 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
                     if (last) break :daemon_loop;
                     continue;
                 }
+                client.legacy_tail_candidate = false;

                 while (client.read_buf.next()) |msg| {
+                    switch (msg.header.tag) {
+                        .Tail, .Init, .Capabilities, .Run => {},
+                        else => client.ignoreDeferredPtyOutput(gpa),
+                    }
                     switch (msg.header.tag) {
                         .Input => try daemon.handleInput(gpa, client, msg.payload),
                         .Send => daemon.handleSend(gpa, msg.payload),
                         .Output => try daemon.handleOutput(gpa, msg.payload, &term, &vt_stream),
-                        .Init => try daemon.handleInit(gpa, io, client, pty_fd, &term, msg.payload),
+                        .Init => try daemon.handleInit(
+                            gpa,
+                            io,
+                            client,
+                            pty_fd,
+                            &term,
+                            &vt_stream,
+                            msg.payload,
+                        ),
                         .Switch => try daemon.handleSwitch(gpa, msg.payload),
                         .Resize => try daemon.handleResize(gpa, client, pty_fd, &term, msg.payload),
                         .Detach => {
@@ -519,8 +695,14 @@ fn daemonLoop(daemon: *Daemon, gpa: std.mem.Allocator, io: std.Io, server_sock_f
                         .LabelGet => try daemon.handleLabelGet(gpa, client),
                         .LabelSet => try daemon.handleLabelSet(gpa, client, msg.payload),
                         .LabelClear => try daemon.handleLabelClear(gpa, client),
-                        .EnvGet => try daemon.handleEnvGet(gpa, client),
+                        .EnvGet => if (msg.payload.len == @sizeOf(ipc.PixelSize))
+                            daemon.handlePixelSize(client, msg.payload)
+                        else
+                            try daemon.handleEnvGet(gpa, client),
                         .EnvSet => try daemon.handleEnvSet(gpa, client, msg.payload),
+                        .Capabilities => daemon.handleCapabilities(gpa, client, msg.payload),
+                        .PixelSize => daemon.handlePixelSize(client, msg.payload),
+                        .Tail => try client.startReceivingPtyOutput(gpa, true),
                         .History => try daemon.handleHistory(gpa, client, &term, msg.payload),
                         .Run => try daemon.handleRun(gpa, io, client, msg.payload),
                         .Ack, .TaskComplete, .LabelData, .EnvData, .Snapshot => {},
@@ -572,11 +754,24 @@ const ClientResult = struct {
 /// Client represents each terminal that has connected to a session.
 ///
 /// Multiple Clients can connect to a single session.
+const PtyOutputState = enum { classifying, snapshot_pending, receiving, ignoring };
+const DEFERRED_PTY_OUTPUT_MAX = 1024 * 1024;
+
 pub const Client = struct {
     alloc: std.mem.Allocator,
     socket_fd: i32,
     has_pending_output: bool = false,
+    pty_output_state: PtyOutputState = .classifying,
+    legacy_tail_candidate: bool = true,
+    legacy_tail_ready_at_ms: i64 = 0,
+    deferred_pty_output: std.ArrayList(u8) = .empty,
+    deferred_pty_output_overflowed: bool = false,
     is_terminal: bool = false, // sent .Init (a `zmx attach`), not a run/send/tail client
+    capabilities: ipc.Capabilities = .{},
+    pixel_size: ipc.PixelSize = .{},
+    /// True once this client has sent a PixelSize message. Until then its
+    /// zero fields are unknown and winsize writes preserve the PTY's pixels.
+    has_pixel_size: bool = false,
     read_buf: ipc.SocketBuffer,
     write_buf: std.ArrayList(u8),
     env_str: ?[]u8 = null,
@@ -585,9 +780,77 @@ pub const Client = struct {
         lib_posix.close(self.socket_fd);
         self.read_buf.deinit();
         self.write_buf.deinit(self.alloc);
+        self.deferred_pty_output.deinit(self.alloc);
         if (self.env_str) |s| gpa.free(s);
     }

+    fn queuePtyOutput(self: *Client, gpa: std.mem.Allocator, payload: []const u8) !void {
+        switch (self.pty_output_state) {
+            .receiving => {
+                try ipc.appendMessage(gpa, &self.write_buf, .Output, payload);
+                self.has_pending_output = true;
+            },
+            .classifying => {
+                if (self.deferred_pty_output_overflowed) return;
+                const framed_len = @sizeOf(ipc.Header) + payload.len;
+                if (framed_len > DEFERRED_PTY_OUTPUT_MAX or
+                    self.deferred_pty_output.items.len > DEFERRED_PTY_OUTPUT_MAX - framed_len)
+                {
+                    std.log.warn(
+                        "deferred PTY output exceeded {d} bytes; dropping it for stalled client fd={d}",
+                        .{ DEFERRED_PTY_OUTPUT_MAX, self.socket_fd },
+                    );
+                    self.releaseDeferredPtyOutput(gpa);
+                    self.deferred_pty_output_overflowed = true;
+                    return;
+                }
+                try ipc.appendMessage(gpa, &self.deferred_pty_output, .Output, payload);
+            },
+            .snapshot_pending, .ignoring => {},
+        }
+    }
+
+    fn startReceivingPtyOutput(
+        self: *Client,
+        gpa: std.mem.Allocator,
+        replay_deferred: bool,
+    ) !void {
+        if (self.pty_output_state == .receiving) return;
+        if (replay_deferred and
+            !self.deferred_pty_output_overflowed and
+            self.deferred_pty_output.items.len > 0)
+        {
+            try self.write_buf.appendSlice(gpa, self.deferred_pty_output.items);
+            self.has_pending_output = true;
+        }
+        self.releaseDeferredPtyOutput(gpa);
+        self.deferred_pty_output_overflowed = false;
+        self.pty_output_state = .receiving;
+        self.legacy_tail_candidate = false;
+    }
+
+    fn discardDeferredPtyOutput(
+        self: *Client,
+        gpa: std.mem.Allocator,
+        next_state: PtyOutputState,
+    ) void {
+        self.releaseDeferredPtyOutput(gpa);
+        self.deferred_pty_output_overflowed = false;
+        self.pty_output_state = next_state;
+        self.legacy_tail_candidate = false;
+    }
+
+    fn ignoreDeferredPtyOutput(self: *Client, gpa: std.mem.Allocator) void {
+        if (self.pty_output_state == .classifying) {
+            self.discardDeferredPtyOutput(gpa, .ignoring);
+        }
+    }
+
+    fn releaseDeferredPtyOutput(self: *Client, gpa: std.mem.Allocator) void {
+        self.deferred_pty_output.deinit(gpa);
+        self.deferred_pty_output = .empty;
+    }
+
     fn setEnv(self: *Client, gpa: std.mem.Allocator, env_str: []const u8) !void {
         if (self.env_str) |s| gpa.free(s);
         self.env_str = if (env_str.len > 0) try gpa.dupe(u8, env_str) else null;
@@ -724,7 +987,7 @@ pub const Daemon = struct {
     /// and run the clientLoop.  If it's the daemon then we need to bail since
     /// the daemonLoop is created inside this fn and when it returns that means
     /// the daemon stopped and needs to exit.
-    pub fn ensureSession(self: *Daemon, io: std.Io) !bool {
+    pub fn ensureSession(self: *Daemon, io: std.Io, announce_create: bool) !bool {
         const sesh_name = self.session_name;
         std.log.info("ensure session session={s}", .{sesh_name});
         var dir = try std.Io.Dir.openDirAbsolute(io, self.cfg.socket_dir, .{});
@@ -765,10 +1028,16 @@ pub const Daemon = struct {
             return false;
         }

-        return self.run(io, dir, sesh_name);
+        return self.run(io, dir, sesh_name, announce_create);
     }

-    fn run(self: *Daemon, io: std.Io, dir: std.Io.Dir, sesh_name: []const u8) !bool {
+    fn run(
+        self: *Daemon,
+        io: std.Io,
+        dir: std.Io.Dir,
+        sesh_name: []const u8,
+        announce_create: bool,
+    ) !bool {
         std.log.info("creating session={s}", .{sesh_name});
         const server_sock_fd: lib_posix.socket_t = try socket.createSocket(self.socket_path);
         const log_fd = log.log_system.file.?.handle;
@@ -799,11 +1068,12 @@ pub const Daemon = struct {
         ) catch |err| {
             switch (err) {
                 error.IsClientProc => {
-                    // send a msg to the client that the session was created.
-                    var w_buf: [2048]u8 = undefined;
-                    var w = std.Io.File.stdout().writer(io, &w_buf);
-                    try w.interface.print("session \"{s}\" created\n", .{sesh_name});
-                    try w.interface.flush();
+                    if (announce_create) {
+                        var w_buf: [2048]u8 = undefined;
+                        var w = std.Io.File.stdout().writer(io, &w_buf);
+                        try w.interface.print("session \"{s}\" created\n", .{sesh_name});
+                        try w.interface.flush();
+                    }
                     lib_posix.close(server_sock_fd);
                     return false;
                 },
@@ -986,9 +1256,23 @@ pub const Daemon = struct {
         client: *Client,
         pty_fd: i32,
         term: *ghostty_vt.Terminal,
+        vt_stream: *ghostty_vt.TerminalStream,
         payload: []const u8,
     ) !void {
         client.is_terminal = true;
+        client.discardDeferredPtyOutput(gpa, .snapshot_pending);
+
+        // Graftty 0.6 used tag 18 for Capabilities. New clients interpret
+        // this server-to-client shape as the legacy advertisement, while
+        // older clients see the exact tag they expect.
+        const legacy_capabilities = ipc.Capabilities{ .bits = ipc.Capabilities.pixel_size };
+        try ipc.appendMessage(
+            gpa,
+            &client.write_buf,
+            .Send,
+            std.mem.asBytes(&legacy_capabilities),
+        );
+        client.has_pending_output = true;

         if (self.leader_client_fd == null) {
             try self.setLeader(gpa, client);
@@ -1004,8 +1288,14 @@ pub const Daemon = struct {
             const resize = std.mem.bytesToValue(ipc.Resize, payload);
             rows = resize.rows;
             cols = resize.cols;
+        } else if (payload.len == @sizeOf(ipc.LegacyResize)) {
+            const resize = std.mem.bytesToValue(ipc.LegacyResize, payload);
+            rows = resize.rows;
+            cols = resize.cols;
             xpixel = resize.xpixel;
             ypixel = resize.ypixel;
+            client.pixel_size = .{ .xpixel = xpixel, .ypixel = ypixel };
+            client.has_pixel_size = true;
         } else {
             var reader: std.Io.Reader = .fixed(payload);
             if (snapshot.startImport(gpa, io, &reader, 1024 * 1024)) |import_res| {
@@ -1018,6 +1308,8 @@ pub const Daemon = struct {
                 cols = imported_term.cols;
                 xpixel = @as(u16, @intCast(@min(std.math.maxInt(u16), imported_term.width_px)));
                 ypixel = @as(u16, @intCast(@min(std.math.maxInt(u16), imported_term.height_px)));
+                client.pixel_size = .{ .xpixel = xpixel, .ypixel = ypixel };
+                client.has_pixel_size = true;

                 // First client attaching seeds the base session theme, cursor style/blink, modes, and pixel geometry
                 if (!self.has_had_client) {
@@ -1034,55 +1326,56 @@ pub const Daemon = struct {
             }
         }

-        // Export terminal snapshot BEFORE resize to capture correct cursor position.
-        // Resizing triggers reflow which can move the cursor, and the shell's
-        // SIGWINCH-triggered redraw will run after our snapshot is sent.
-        // Only serialize on re-attach (has_had_client), not first attach, to avoid
-        // interfering with shell initialization (DA1 queries, etc.)
-        if (self.has_pty_output and self.has_had_client) {
+        // Export terminal state before resize so the initial replay is a
+        // single cut: output observed before Init is represented by the
+        // snapshot, and only later PTY output is broadcast live.
+        const has_initial_state = self.has_pty_output;
+        const wants_initial_raw_snapshot = client.capabilities.supportsRawSnapshotStream();
+        const should_send_snapshot = client.capabilities.supportsSnapshot();
+        const snapshot_sent = if (should_send_snapshot) blk: {
             const cursor = &term.screens.active.cursor;
             std.log.debug(
                 "cursor before serialize: x={d} y={d} pending_wrap={}",
                 .{ cursor.x, cursor.y, cursor.pending_wrap },
             );
-            var snap_buf: std.Io.Writer.Allocating = .init(gpa);
-            defer snap_buf.deinit();
-            snapshot.exportSnapshot(gpa, &snap_buf.writer, term, .{}) catch |err| {
+            appendTerminalSnapshot(gpa, client, term, vt_stream) catch |err| {
                 std.log.warn("failed to export snapshot err={s}", .{@errorName(err)});
+                break :blk false;
             };
-            if (snap_buf.written().len > 0) {
-                std.log.debug("serialize terminal snapshot bytes={d}", .{snap_buf.written().len});
-                ipc.appendMessage(gpa, &client.write_buf, .Snapshot, snap_buf.written()) catch |err| {
-                    std.log.warn(
-                        "failed to buffer snapshot for client err={s}",
-                        .{@errorName(err)},
-                    );
-                };
+            break :blk true;
+        } else false;
+
+        if (should_send_snapshot and !snapshot_sent) {
+            // An empty Snapshot is an explicit client-local negotiation
+            // failure. Raw clients exit, while interactive clients accept the
+            // legacy VT replay queued below.
+            try ipc.appendMessage(gpa, &client.write_buf, .Snapshot, "");
+            client.has_pending_output = true;
+        }
+
+        if (wants_initial_raw_snapshot and !snapshot_sent) {
+            return;
+        }
+
+        if (has_initial_state and !snapshot_sent) {
+            if (util.serializeTerminalState(gpa, term)) |term_output| {
+                std.log.debug("serialize terminal state for legacy client", .{});
+                const restore_data = util.rewritePromptRedraw(gpa, term_output) orelse term_output;
+                defer gpa.free(term_output);
+                defer if (restore_data.ptr != term_output.ptr) gpa.free(restore_data);
+                try ipc.appendMessage(gpa, &client.write_buf, .Output, restore_data);
                 client.has_pending_output = true;
             }
+            appendTerminalContinuation(gpa, client, vt_stream) catch |err| {
+                std.log.warn("failed to serialize terminal continuation err={s}", .{@errorName(err)});
+            };
         }

         // only resize if leader
         if (is_leader) {
-            var ws: cross.c.struct_winsize = .{
-                .ws_row = rows,
-                .ws_col = cols,
-                .ws_xpixel = xpixel,
-                .ws_ypixel = ypixel,
-            };
+            const ws = clientWinsize(client, pty_fd, rows, cols);
             _ = cross.c.ioctl(pty_fd, cross.c.TIOCSWINSZ, &ws);

-            // On re-attach, deliver SIGWINCH to the foreground process group so
-            // incremental renderers (Ink, Claude Code, etc.) know to repaint.
-            // If the size changed, TIOCSWINSZ above already sent SIGWINCH; if the size
-            // was unchanged, the kernel suppressed it, so signal the pgrp explicitly.
-            if (self.has_pty_output and self.has_had_client) {
-                var pgrp: lib_posix.pid_t = 0;
-                if (cross.c.ioctl(pty_fd, cross.c.TIOCGPGRP, &pgrp) == 0 and pgrp > 0) {
-                    lib_posix.kill(-pgrp, .WINCH) catch {};
-                }
-            }
-
             // Disable prompt_redraw before resize. The daemon's internal terminal
             // would otherwise clear prompt lines expecting the shell to redraw them,
             // but the shell's redraw goes to the PTY (forwarded to clients), not to
@@ -1099,8 +1392,53 @@ pub const Daemon = struct {
             // Mark that we've had a client init, so subsequent clients get terminal state
             self.has_had_client = true;

-            std.log.debug("init resize rows={d} cols={d}", .{ rows, cols });
+            std.log.debug(
+                "init resize rows={d} cols={d} xpixel={d} ypixel={d}",
+                .{ rows, cols, ws.ws_xpixel, ws.ws_ypixel },
+            );
+        }
+        try client.startReceivingPtyOutput(gpa, false);
+    }
+
+    pub fn handleCapabilities(
+        _: *Daemon,
+        gpa: std.mem.Allocator,
+        client: *Client,
+        payload: []const u8,
+    ) void {
+        if (payload.len != @sizeOf(ipc.Capabilities)) return;
+        client.capabilities = std.mem.bytesToValue(ipc.Capabilities, payload);
+        if (client.capabilities.supportsSnapshot()) {
+            client.discardDeferredPtyOutput(gpa, .snapshot_pending);
+        }
+    }
+
+    pub fn handlePixelSize(_: *Daemon, client: *Client, payload: []const u8) void {
+        if (payload.len != @sizeOf(ipc.PixelSize)) return;
+        client.pixel_size = std.mem.bytesToValue(ipc.PixelSize, payload);
+        client.has_pixel_size = true;
+    }
+
+    fn clientWinsize(
+        client: *const Client,
+        pty_fd: i32,
+        rows: u16,
+        cols: u16,
+    ) cross.c.struct_winsize {
+        var ws: cross.c.struct_winsize = .{
+            .ws_row = rows,
+            .ws_col = cols,
+            .ws_xpixel = client.pixel_size.xpixel,
+            .ws_ypixel = client.pixel_size.ypixel,
+        };
+        if (!client.has_pixel_size) {
+            var current: cross.c.struct_winsize = undefined;
+            if (cross.c.ioctl(pty_fd, cross.c.TIOCGWINSZ, &current) == 0) {
+                ws.ws_xpixel = current.ws_xpixel;
+                ws.ws_ypixel = current.ws_ypixel;
+            }
         }
+        return ws;
     }

     pub fn handleResize(
@@ -1111,18 +1449,32 @@ pub const Daemon = struct {
         term: *ghostty_vt.Terminal,
         payload: []const u8,
     ) !void {
-        if (payload.len != @sizeOf(ipc.Resize)) return;
+        var rows: u16 = undefined;
+        var cols: u16 = undefined;
+        if (payload.len == @sizeOf(ipc.Resize)) {
+            const resize = std.mem.bytesToValue(ipc.Resize, payload);
+            rows = resize.rows;
+            cols = resize.cols;
+        } else if (payload.len == @sizeOf(ipc.LegacyResize)) {
+            const resize = std.mem.bytesToValue(ipc.LegacyResize, payload);
+            rows = resize.rows;
+            cols = resize.cols;
+            client.pixel_size = .{ .xpixel = resize.xpixel, .ypixel = resize.ypixel };
+            client.has_pixel_size = true;
+        } else return;
         if (self.leader_client_fd == null) {
             try self.setLeader(gpa, client);
         }
         // only leader can resize
         if (self.leader_client_fd != client.socket_fd) return;

-        const resize = std.mem.bytesToValue(ipc.Resize, payload);
-        var ws = resize.winsize();
+        const ws = clientWinsize(client, pty_fd, rows, cols);
         _ = cross.c.ioctl(pty_fd, cross.c.TIOCSWINSZ, &ws);
-        try resizeTerm(gpa, term, resize.cols, resize.rows);
-        std.log.debug("resize rows={d} cols={d}", .{ resize.rows, resize.cols });
+        try resizeTerm(gpa, term, cols, rows);
+        std.log.debug(
+            "resize rows={d} cols={d} xpixel={d} ypixel={d}",
+            .{ rows, cols, ws.ws_xpixel, ws.ws_ypixel },
+        );
     }

     pub fn handleDetach(self: *Daemon, gpa: std.mem.Allocator, client: *Client, i: usize) void {
@@ -1229,6 +1581,8 @@ pub const Daemon = struct {
     }

     pub fn handleRun(self: *Daemon, gpa: std.mem.Allocator, io: std.Io, client: *Client, payload: []const u8) !void {
+        client.discardDeferredPtyOutput(gpa, .ignoring);
+        try client.startReceivingPtyOutput(gpa, false);
         // Reset task tracking so the new command's exit marker is detected.
         // Without this, a second `zmx run` on the same session is ignored
         // because task_exit_code is still set from the first run.
@@ -1315,8 +1669,7 @@ pub const Daemon = struct {
         self.setPwd(term);
         self.has_pty_output = true;
         for (self.clients.items) |client| {
-            try ipc.appendMessage(gpa, &client.write_buf, .Output, payload);
-            client.has_pending_output = true;
+            try client.queuePtyOutput(gpa, payload);
         }
         if (self.clients.items.len > 0) {
             lib_posix.kill(self.pid, lib_posix.SIG.WINCH) catch |err| {
diff --git a/src/main.zig b/src/main.zig
index e8d6725..9605b1d 100644
--- a/src/main.zig
+++ b/src/main.zig
@@ -15,7 +15,6 @@ const Cfg = @import("cfg.zig");
 const loop = @import("loop.zig");
 const Client = loop.Client;
 const Daemon = loop.Daemon;
-const probe = @import("probe.zig");
 const version = build_options.version;
 const ghostty_version = build_options.ghostty_version;

@@ -385,6 +384,16 @@ pub fn main(init: std.process.Init) !void {
                 error.OutOfMemory => return err,
             };
             const client_sock = try socket.sessionConnect(socket_path);
+            ipc.send(client_sock, .Tail, "") catch |err| switch (err) {
+                error.ConnectionResetByPeer, error.BrokenPipe => {
+                    lib_posix.close(client_sock);
+                    continue;
+                },
+                else => {
+                    lib_posix.close(client_sock);
+                    return err;
+                },
+            };
             try client_socket_fds.append(gpa, client_sock);
         }
         _ = try tail(gpa, client_socket_fds, false, false);
@@ -1540,7 +1549,7 @@ fn attach(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, env_str: []const
         return switchSesh(gpa, io, daemon, sesh);
     }

-    const is_daemon_proc = try daemon.ensureSession(io);
+    const is_daemon_proc = try daemon.ensureSession(io, !snapshot_mode);
     if (is_daemon_proc) return;

     // The session exists now, so labels land before the client takes over the
@@ -1565,7 +1574,10 @@ fn attach(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, env_str: []const
     // skip terminal setup entirely rather than applying undefined stack bytes
     // via tcsetattr.
     var orig_termios: cross.c.termios = undefined;
-    const stdin_is_tty = !snapshot_mode and cross.c.tcgetattr(lib_posix.STDIN_FILENO, &orig_termios) == 0;
+    const stdin_is_tty = cross.c.tcgetattr(lib_posix.STDIN_FILENO, &orig_termios) == 0;
+    var orig_stdout_termios: cross.c.termios = undefined;
+    const snapshot_stdout_is_tty = snapshot_mode and
+        cross.c.tcgetattr(lib_posix.STDOUT_FILENO, &orig_stdout_termios) == 0;

     // RIS, OSC 10/11/12
     const restore_seq = "\x1bc\x1b]110\x1b\\\x1b]111\x1b\\\x1b]112\x1b\\";
@@ -1574,12 +1586,21 @@ fn attach(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, env_str: []const
         if (stdin_is_tty) {
             _ = cross.c.tcsetattr(lib_posix.STDIN_FILENO, cross.c.TCSAFLUSH, &orig_termios);
         }
+        if (snapshot_stdout_is_tty) {
+            _ = cross.c.tcsetattr(lib_posix.STDOUT_FILENO, cross.c.TCSANOW, &orig_stdout_termios);
+        }
         if (!snapshot_mode) {
             // Reset terminal modes on detach
             _ = lib_posix.write(lib_posix.STDOUT_FILENO, restore_seq) catch {};
         }
     }

+    if (snapshot_stdout_is_tty) {
+        var raw_stdout_termios = orig_stdout_termios;
+        cross.c.cfmakeraw(&raw_stdout_termios);
+        _ = cross.c.tcsetattr(lib_posix.STDOUT_FILENO, cross.c.TCSANOW, &raw_stdout_termios);
+    }
+
     if (stdin_is_tty) {
         var raw_termios = orig_termios;
         //  set raw mode after successful connection.
@@ -1598,20 +1619,6 @@ fn attach(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, env_str: []const
         _ = cross.c.tcsetattr(lib_posix.STDIN_FILENO, cross.c.TCSANOW, &raw_termios);
     }

-    // Probe outer terminal (if TTY) and create init snapshot
-    const size = ipc.getTerminalSize(lib_posix.STDOUT_FILENO);
-    const init_snap = try probe.probeAndSnapshot(
-        io,
-        gpa,
-        size.rows,
-        size.cols,
-        size.xpixel,
-        size.ypixel,
-        stdin_is_tty,
-        probe.DEFAULT_PROBE_TIMEOUT_MS,
-    );
-    defer gpa.free(init_snap);
-
     if (!snapshot_mode) {
         // Clear screen before attaching. This provides a clean slate before
         // the session restore.
@@ -1619,7 +1626,7 @@ fn attach(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, env_str: []const
         _ = try lib_posix.write(lib_posix.STDOUT_FILENO, clear_seq);
     }

-    const looper = try loop.clientLoop(io, client_sock, snapshot_mode, init_snap, env_str);
+    const looper = try loop.clientLoop(io, client_sock, snapshot_mode, env_str);
     switch (looper.kind) {
         .detach => return,
         .switch_session => {
@@ -1656,7 +1663,7 @@ fn attach(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, env_str: []const
 }

 fn writeFile(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, file_path: []const u8) !void {
-    const is_daemon_proc = try daemon.ensureSession(io);
+    const is_daemon_proc = try daemon.ensureSession(io, true);
     if (is_daemon_proc) return;

     var buf: [4096]u8 = undefined;
@@ -1808,7 +1815,7 @@ fn run(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, detached: bool, comm
     var allocated_cmd: ?[]u8 = null;
     defer if (allocated_cmd) |cmd| gpa.free(cmd);

-    const is_daemon_proc = try daemon.ensureSession(io);
+    const is_daemon_proc = try daemon.ensureSession(io, true);
     if (is_daemon_proc) return;

     if (command_args.len > 0) {
@@ -1876,7 +1883,8 @@ fn run(gpa: std.mem.Allocator, io: std.Io, daemon: *Daemon, detached: bool, comm
     defer lib_posix.close(client_sock);

     const term_size = ipc.getTerminalSize(lib_posix.STDOUT_FILENO);
-    ipc.send(client_sock, .Resize, std.mem.asBytes(&term_size)) catch {};
+    const resize = term_size.resize();
+    ipc.send(client_sock, .Resize, std.mem.asBytes(&resize)) catch {};

     var fds = try std.ArrayList(i32).initCapacity(gpa, 1);
     defer fds.deinit(gpa);
diff --git a/src/posix.zig b/src/posix.zig
index 23daed6..277ce73 100644
--- a/src/posix.zig
+++ b/src/posix.zig
@@ -71,6 +71,26 @@ pub fn sigemptyset() sigset_t {
     return system.sigemptyset();
 }

+pub fn sigaddset(set: *sigset_t, sig: SIG) void {
+    if (builtin.link_libc) {
+        switch (errno(system.sigaddset(set, sig))) {
+            .SUCCESS => return,
+            else => unreachable,
+        }
+    }
+    system.sigaddset(set, sig);
+}
+
+/// Sets the thread signal mask.
+pub fn sigprocmask(flags: u32, noalias set: ?*const sigset_t, noalias oldset: ?*sigset_t) void {
+    switch (errno(system.sigprocmask(@bitCast(flags), set, oldset))) {
+        .SUCCESS => return,
+        .FAULT => unreachable,
+        .INVAL => unreachable,
+        else => unreachable,
+    }
+}
+
 /// Examine and change a signal action.
 pub fn sigaction(sig: SIG, noalias act: ?*const Sigaction, noalias oact: ?*Sigaction) void {
     switch (errno(system.sigaction(sig, act, oact))) {
