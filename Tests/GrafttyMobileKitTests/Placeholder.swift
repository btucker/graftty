#if canImport(UIKit)
import XCTest
@testable import GrafttyMobileKit

// Placeholder test file — populated in later tasks alongside the
// corresponding implementation files. This file is deleted in Task 14.
#endif
